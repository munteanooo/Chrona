import { Button, Card } from "@chrona/ui";

export default function DashboardPage() {
  return (
    <main>
      <h1>Dashboard</h1>

      <Card>
        <p>Chrona Task</p>

        <Button>
          Create Task
        </Button>
      </Card>
    </main>
  );
}